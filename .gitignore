# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or fail
#   all together. If this is the case, uncomment the following line.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducible builds.
#   See https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a Python
#  project it can be shared to the team, therefore have it be followed here.
.idea/

# Editor / OS
.DS_Store
*.swp
*.swo
*~
.vscode/

# Hermes runtime & local state
auth.json
auth.lock
config.yaml
SOUL.md
channel_directory.json
workspace-sessions.json
context_length_cache.yaml
gateway_state.json
gateway.pid
gateway.lock
processes.json
tasks.json
sessions.db
sessions.db-*
state.db
state.db-*
response_store.db
response_store.db-*
kanban.db
kanban.db-*
memories/
cron/
cache/
logs/
sessions/
image_cache/
audio_cache/
images/
bin/
sandboxes/
pastes/
.hermes_history
hermes.log
*.session
*.credentials

# Node / Web UI (built assets + dependencies)
node_modules/
dist/
build/
.next/
.turbo/
package-lock.json
yarn.lock
pnpm-lock.yaml
web/dist/
web/build/
web/node_modules/

# Bundled dependencies downloaded at build/install time
hermes_cli/bin/
# Keep root bin (used for local wrappers), only ignore sub-binaries
# bin/*
# !bin/README.md

# Testing
tests/tmp/
tests/.pytest_cache/
.coverage
htmlcov/
*.lprof

# Infographics generated assets
#
# IMPORTANT: do NOT commit generated infographic images or HTML to git.
# They are user output artifacts produced at runtime, not repository source.
# A CI check (.github/workflows/infographic-check.yml) enforces this.
#
# Spelling variants are listed because a single `infographic/` pattern was
# sidestepped by an `infograficos/` directory (#70552). .gitignore is only
# the first line of defence and cannot stop `git add -f` at all — the
# infographic-check CI job is what actually enforces this.
infographic/
infographics/
infograficos/
infografico/
native/fts5_cjk/*.so
graphify-out/

# Custom markdown files
PRD.md
modification.md

# AG bypass proxy (local-only, never commit)
scripts/ag_proxy.py
scripts/route-provider.sh
scripts/setup-bypass.sh

# Runtime marker written by hermes update when a lazy dependency refresh is
# interrupted; consumed by launch-time recovery. Never commit it (was tracked
# by accident via 3a69e34702, removed in the #72002 salvage).
.lazy-refresh-incomplete
.skills_prompt_snapshot.json

# Disposable profile created by scripts/probe_active_session_exclusivity.py
.probe-home/
